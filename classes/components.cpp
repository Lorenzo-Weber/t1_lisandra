#include <iostream>
#include <cmath>
#include <GL/glut.h>
#include <GL/freeglut_ext.h> 
#include "gl_canvas2d.h"

using namespace std;

class Button {
public:
 

    // Button(float width, float height, float x, float y, string text = "Default")
    //     : width(width), height(height), x(x), y(y), text(text), hovering(false), clicked(false)
    // {

    // }

    void hover() {

    }

    void click() {

    }

    void draw() {

    }

    void run() {

    }
};

class SearchBar {
public:


    // SearchBar(float width, float height, float x, float y)
    //     : width(width), height(height), x(x), y(y), hovering(false), clicked(false), text(""), typing(false), keyPressed(false)
    // {

    // }

    void hover( ) {

    }

    void click( ) {

    }

    void write() {

    }

    void draw() {
  
    }

    void run() {


    }
};
